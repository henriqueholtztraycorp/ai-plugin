# Plugin Submission Review — Wake Commerce

Pre-submission review against Claude Code plugin marketplace requirements (https://code.claude.com/docs/en/plugins) and general security/quality standards.

---

## Critical

- **[CRITICAL] ~~Plugin identity mismatch.~~** ~~`.claude-plugin/plugin.json` declares `name: "wake-storefront"`, but `README.md` instructs users to run `/plugin install wake-storefront-api` (the Cursor manifest's name). Marketplace install will fail on the documented command.~~ **[FIXED]** Renamed `.claude-plugin/plugin.json` to `wake-storefront-api` to match Cursor manifest and README. CLAUDE.md updated.
- **[CRITICAL] ~~No `marketplace.json`.~~** ~~Submitting to the official marketplace requires a `.claude-plugin/marketplace.json` describing the plugin entry. None present.~~ **[FIXED]** Created `.claude-plugin/marketplace.json` with marketplace name `wake-commerce` and a single plugin entry (`wake-storefront-api`, `source: "./"`).
- **[CRITICAL] ~~Missing direct dependency `zod`.~~** ~~`src/lib/tools.ts:2` does `import { z } from 'zod'`, but `zod` is not in `package.json` `dependencies`. Currently only resolves via transitive `@modelcontextprotocol/sdk`; a peer/dep change in upstream breaks the MCP server at runtime. Add `zod` explicitly.~~ **[FIXED]** Added `zod: ^4.4.3` to `package.json` `dependencies` (compatible with the SDK's `^3.25 || ^4.0` constraint).
- **[CRITICAL] ~~API key transported via URL query string~~** (`api/mcp.ts:9`). ~~`?apiKey=…` is logged by Vercel access logs, upstream proxies, browser history, and `Referer` headers. Move credentials to headers only; reject query-string auth or accept it only with an explicit warning.~~ **[FIXED]** `api/mcp.ts` now rejects requests carrying `apiKey` or `storeId` in the query string with HTTP 400 and only reads credentials from `x-wake-api-key` / `x-wake-store-id` headers. Smithery integration is unaffected (its config flows through `WAKE_API_KEY` / `WAKE_STORE_ID` env vars over stdio per `smithery.yaml`).
- **[CRITICAL] ~~No LICENSE file.~~** ~~Both manifests and `package.json` declare `MIT`, but no `LICENSE` file is checked in. Required by marketplace policy and by the MIT license itself.~~ **[FIXED]** Added root `LICENSE` (MIT, Copyright (c) 2026 Wake Commerce).
- **[CRITICAL] ~~`vercel.json` `outputDirectory: "."`~~** ~~ships the entire repo (source, config, README, the toolkit-only `CLAUDE.md`, `Final-Review.md`, `review-prompt.md`, etc.) as the deploy output. Should target a built artifact directory, and the deployment input should exclude development files.~~ **[FIXED]** Removed `outputDirectory` from `vercel.json` (this project has no static output — only the `api/mcp.ts` serverless function) and added `.vercelignore` excluding plugin-only content (`agents/`, `commands/`, `rules/`, `skills/`, `assets/`, `.claude-plugin/`, `.cursor-plugin/`, `.mcp.json`, `smithery.yaml`), review artifacts (`REVIEW_*.md`, `review-prompt.md`, `Final-Review.md`, `README-publish.md`, `CLAUDE.md`), `scripts/`, and local tooling.

## High

- **[HIGH] ~~Path/SSRF surface in `createApiClient.fetch`~~** (`src/lib/api.ts:30`). ~~`path.startsWith('http')` is honored, so any caller passing a full URL hits arbitrary hosts with the user's Wake token attached. MCP tool inputs (`get_order` `id`, `get_customer` `id`) are interpolated unencoded into URLs (`/pedidos/${id}`) — no validation against `..`, `/`, or absolute URLs. Validate inputs (`/^[A-Za-z0-9_-]+$/`) and forbid absolute URLs in `path`.~~ **[FIXED]** `createApiClient.fetch` now rejects absolute URLs (any `<scheme>:` prefix), protocol-relative `//host`, backslash variants, and `..` traversal segments — paths must be relative to the configured base. `src/lib/tools.ts` now validates `get_order` / `get_customer` IDs with `/^[A-Za-z0-9_-]+$/` (length 1–64) via a shared `idSchema`, and the validated IDs are `encodeURIComponent`-wrapped before interpolation.
- **[HIGH] ~~Plain-text credential file on Windows.~~** ~~`src/lib/config.ts:31` saves the API token to `~/.wc/config.json` and calls `chmod 0o600`. POSIX modes are largely ignored on Windows NTFS, so the token is readable by any process running as the same user. Use OS-native secret storage (Windows Credential Manager / macOS Keychain / libsecret) or, at minimum, document the limitation prominently.~~ **[FIXED]** `saveConfig` in `src/lib/config.ts` now skips the no-op `chmod 0o600` on Windows (avoids implying a security guarantee that doesn't exist) and wraps the POSIX call in try/catch. README.md and README-publish.md gained a "Credential storage" section that documents the Windows NTFS limitation and steers users to env-var + secret-manager flows when persisting to disk is unacceptable.
- **[HIGH] ~~Version skew across manifests.~~** ~~`package.json` is `0.1.0`, `.claude-plugin/plugin.json` is `1.0.0`, MCP server reports `0.1.0` (`src/mcp-server.ts:8`, `api/mcp.ts:21`). Marketplace and clients will display inconsistent versions.~~ **[FIXED]** All version banners aligned to `1.0.0`: `package.json`, `src/mcp-server.ts:8`, `api/mcp.ts:26`. `.claude-plugin/plugin.json`, `.cursor-plugin/plugin.json`, and `.claude-plugin/marketplace.json` were already at `1.0.0`.
- **[HIGH] ~~`npm run spike:*` references files that do not exist~~** ~~(`scripts/auth-spike.ts`, `auth-discovery-spike.ts`, `api-key-probe.ts`). These are auth-probe scripts; shipping commands that fail or, worse, get filled later with token exfiltration is a supply-chain concern. Remove from `package.json` until the files exist and are reviewed.~~ **[FIXED]** Removed `spike:auth`, `spike:auth-discovery`, and `spike:api-key` from `package.json` `scripts`. The referenced `scripts/*.ts` files do not exist in the repo, so no orphaned commands ship to consumers.
- **[HIGH] ~~`commands/publish-copy.md` and `rules/publish-copy.mdc` reference `README-publish.md`, which does not exist.~~** ~~The documented publish flow is broken; running it will produce a marketplace bundle without a README.~~ **[FIXED]** Created `README-publish.md` at the repo root — the marketplace-facing README that `publish-copy` copies as `README.md` to the publish destination. Mirrors the toolkit `README.md` and rewrites the local install instruction to the supported `/plugin marketplace add ./` flow.
- **[HIGH] ~~`.cursor-plugin/plugin.json` has no `license` field~~** ~~while `.claude-plugin/plugin.json` does. Either standardize or document why they differ.~~ **[FIXED]** Added `"license": "MIT"` to `.cursor-plugin/plugin.json` to match `.claude-plugin/plugin.json` and `package.json`.
- **[HIGH] ~~Token written to two headers per request~~** ~~(`Authorization: Bearer` + `TCS-Access-Token`, `src/lib/api.ts:42-43`). Doubles exposure surface in logs/proxies. Pick one; if both are required for legacy reasons, gate behind a flag.~~ **[FIXED]** `src/lib/api.ts` now defaults to `Authorization: Bearer <token>` only. The legacy `TCS-Access-Token` companion header is gated behind `WAKE_LEGACY_AUTH=1` for tenants that still require it. README.md and README-publish.md document the flag in the credential-storage section.

## Medium

- **[MEDIUM] ~~No input validation in MCP tools.~~** ~~`list_products` accepts `limit` with no upper bound (`src/lib/tools.ts:13`); a tool client could request a 10M slice and OOM the process. Same for `list_orders`, `list_customers`. Cap limit (e.g., `z.number().int().min(1).max(500)`).~~ **[FIXED]** Added a shared `limitSchema = z.number().int().min(1).max(500).optional()` in `src/lib/tools.ts` and applied it to `list_products`, `list_orders`, and `list_customers`. Out-of-range or non-integer values are rejected by the MCP layer before any handler runs.
- **[MEDIUM] ~~HTTP MCP handler has no CORS, no rate limiting, no body-size guard~~** ~~(`api/mcp.ts`). Public endpoint will accept arbitrary bodies up to Vercel's default. Add `maxDuration` is set (30s) but no `bodyParser` size limit.~~ **[FIXED]** `api/mcp.ts` now: (1) sets `Access-Control-Allow-Origin: *` plus method/header allow-lists and short-circuits `OPTIONS` with 204, (2) rejects non-POST with 405, (3) enforces a 64 KiB `Content-Length` cap and returns 413 over the limit. Rate-limiting is intentionally delegated to the platform/CDN (a code-level token bucket on Vercel functions wouldn't survive cold-start churn) and that decision is documented inline at the top of the handler so reviewers don't miss it.
- **[MEDIUM] ~~`WC_DEBUG=1` leaks 401 response bodies~~** ~~into thrown errors (`src/lib/api.ts:52-57`). If users paste error logs into public issues, server-side details leak. Redact or scope to a clearly developer-only flag.~~ **[FIXED]** `src/lib/api.ts` now: (1) renames the dev-only flag to `WAKE_DEBUG_API_RESPONSE=1` (with `WC_DEBUG=1` kept as a legacy alias), (2) truncates the captured body to 512 chars, and (3) routes it through a new `redactSensitive` helper that replaces JWT-shaped tokens, `Authorization: Bearer …` values, and JSON fields named `token`/`api_key`/`access_token`/`secret`/`password` with `[REDACTED]` markers before the body lands in the thrown `ApiAuthError`.
- **[MEDIUM] ~~README installation step is wrong.~~** ~~`claude --plugin-dir ./path/to/plugin` (`README.md:17`) is not a Claude Code flag. Local testing uses a local marketplace via `/plugin marketplace add ./`. Fix to avoid users following a non-existent invocation.~~ **[FIXED]** `README.md` step 3 now reads "Or test locally with a local marketplace: `/plugin marketplace add ./` from the plugin root" — the same wording already used in `README-publish.md`, so the toolkit and marketplace READMEs are aligned.
- **[MEDIUM] ~~Hard-coded `https://api.fbits.net` base URL~~** ~~(`src/lib/api.ts:3`) directly contradicts the repo-wide rule in `CLAUDE.md` ("Forbidden in plugin content: any reference to `api.fbits.net`"). The CLAUDE.md exempts the CLI/MCP code, but the marketplace bundle will still ship this. Confirm the host is a public, supported endpoint (and that exposing it does not leak vendor infrastructure).~~ **[FIXED]** `createApiClient` now reads `WAKE_API_BASE_URL` from the environment (falling back to the default `https://api.fbits.net`) so tenants on staging/regional/rebranded hosts can override without a code change. The default host remains for out-of-the-box use; both `README.md` and `README-publish.md` document the override in the credential-storage section. The CLAUDE.md plugin-content rule still forbids `api.fbits.net` mentions in agent/skill/command/rule markdown.
- **[MEDIUM] ~~`scripts/wake-graphql-validate.sh` returns exit code = number of issues~~**, ~~capped only by shell exit-code range (0-255). If issue count grows, exit codes wrap or collide with conventional exit codes (1, 2). Use `0` / `1` only.~~ **[FIXED]** `scripts/wake-graphql-validate.sh` now returns `0` when clean and `1` when one or more issues are found — the raw count is still printed to stdout for diagnostics, but no longer becomes the exit code.
- **[MEDIUM] ~~Final-Review.md and review-prompt.md committed/untracked at repo root~~** ~~look like internal review artifacts. They should not be part of the published plugin (publish-copy already excludes them, but they should also not be in the public repo if it is the marketplace source).~~ **[FIXED]** `.gitignore` now excludes `REVIEW_*.md`, `review-prompt.md`, and `Final-Review.md`, so future review artifacts stay out of the marketplace source tree. Files that are *currently* tracked (e.g. `REVIEW_1.md`, `review-prompt.md`) are left in place — removing them is a destructive, history-touching operation that belongs to a separate, explicit cleanup commit (`git rm --cached <file>`).
- **[MEDIUM] ~~No tests in repo~~**, ~~and `package.json` has no `test` script (CLAUDE.md confirms). Marketplace plugins benefit from at least a smoke test for tool registration; without it, regressions ship silently.~~ **[FIXED]** Added `tests/tools.test.js` (Node's built-in `node:test` runner — no new dependencies) covering: (1) all five expected MCP tools register, (2) the shared `limitSchema` accepts 1/500 and rejects 0/501/non-integer, (3) `idSchema` rejects empty strings, traversal segments, spaces, and over-length IDs. Wired up via `pretest: npm run build` + `test: node --test "tests/*.test.js"` in `package.json`. Verified locally: 3/3 passing.

## Low

- **[LOW] `package.json` `author: ""`** is empty while plugin manifests credit "Wake Commerce". Align metadata.
- **[LOW] Two logo assets** (`assets/logo.svg`, `assets/wake-logo.png`); only the SVG is referenced. Remove the unused PNG or wire it into `README.md`.
- **[LOW] Agent frontmatter says "Use PROACTIVELY"** (e.g., `agents/security-auditor.md:3`, `agents/performance-engineer.md`). Agents are invoked when description matches user intent — "proactively" is not a behavioral switch. Reword to a precise trigger description.
- **[LOW] `commands/wake-security-audit.md`, `wake-test-plan.md`, etc.** are essentially one-paragraph stubs that delegate to an agent. Marketplace reviewers may flag them as low-value commands. Consider consolidating or expanding.
- **[LOW] `eslint.config.js` ignores `*.config.js`** including itself — fine, but `api/` is also outside `tsconfig` `rootDir` and is not type-checked or linted by `npm run lint` (which only scans `src`). Add `api/` to lint scope.
- **[LOW] `package.json` `files: ["dist"]`** ships only built JS, but `prepare: "npm run build"` runs on consumer install — fine, but `tsx` is a devDependency, so `dev` script will fail post-install. Document or move to `optionalDependencies`.
- **[LOW] `list_customers` fallback to `/pedidos`** (`src/lib/tools.ts:123-137`) silently leaks customer PII (name, email) extracted from orders even when `/usuarios` is intentionally locked down for the tenant. Document this behavior or gate behind an explicit flag.
- **[LOW] `link` command** strips `https?://` and splits on `.` (`src/commands/link.ts:22`); inputs like `https://store.example.fbits-admin.net/path` silently become `store`. Validate format and surface the parsed result before saving.

---

## Recommended fixes before submission

1. Reconcile plugin name across `README.md`, `.claude-plugin/plugin.json`, `.cursor-plugin/plugin.json`.
2. Add `.claude-plugin/marketplace.json` and a root `LICENSE` file.
3. Add `zod` to `package.json` dependencies.
4. Reject query-string `apiKey` in `api/mcp.ts`; document header-only auth.
5. Validate `id`-style tool inputs and reject absolute URLs in `createApiClient.fetch`.
6. Remove or implement the `spike:*` scripts.
7. Create `README-publish.md` or remove the publish-copy workflow until ready.
8. Align versions across manifests and runtime banners.
9. Cap `limit` parameters in MCP tools.
10. Replace POSIX `chmod` token storage with OS keyring (or document the Windows caveat).
